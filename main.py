"""Sentinel entry point for TofuPilot's import detection.

Pytest auto-discovers `test_*.py` files and runs them; nothing here
needs to do anything. This file exists so the deployer's "find an
entry point" check (looking for main.py) succeeds for pytest repos.
"""
